syntax = "proto3";

package store;

message StoreRequest {
    string report_id = 1;
}

message StoreResponse {
    string message = 1;
    bool success = 2;
}

service StoreService {
    rpc saveReport(StoreRequest) returns (StoreResponse) {};
}